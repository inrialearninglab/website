# Installation
- Installation des dépendances
    ```shell
    source .venv/bin/activate && pip3 install -r requirements.txt
    ```
# Visualisation du portail en local
```shell
source .venv/bin/activate && mkdocs serve --watch-theme
```

Après quelques secondes, le portail devrait être visible à l'adresse : http://localhost:8000
